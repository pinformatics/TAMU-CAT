<main class="c-main c-main--dashboard">
  <div class="c-toolbar">
    <div class="c-toolbar__row u-items-center u-justify-between u-w-full u-gap-md">
      <div class="u-shrink-0">
        <%= link_to "← Back", admin_dashboard_path, class: "btn btn-secondary", data: { turbo: false } %>
      </div>
    </div>
  </div>

  <div class="c-page">
    <section class="c-card u-stack u-stack--xl">
      <header class="c-page-header">
        <div>
          <p class="c-eyebrow">People admin</p>
          <h1 class="c-page-title">People Management</h1>
          <p class="c-page-subtitle">Manage member access and student assignments from one workspace.</p>
        </div>
      </header>

      <% tabs = [
        ["Members", "members", "Roles, permissions, and account cleanup"],
        ["Students", "students", "Advisors, tracks, and assignment groups"]
      ] %>

      <div class="u-grid u-grid--2 u-gap-md">
        <% tabs.each do |label, value, description| %>
          <% active = (@people_tab == value) %>
          <%= link_to people_management_path(tab: value), class: [
            "c-card c-card--hover u-block",
            ("is-active" if active)
          ].compact.join(" ") do %>
            <div class="u-responsive-split">
              <div class="u-stack u-stack--xs">
                <div class="c-table__title"><%= label %></div>
                <div class="c-lede"><%= description %></div>
              </div>
              <% if active %>
                <span class="c-status-badge c-status-badge--success">Open</span>
              <% end %>
            </div>
          <% end %>
        <% end %>
      </div>

      <div>
        <% if @people_tab == "students" %>
          <%= render "dashboards/people_students_tab" %>
        <% else %>
          <%= render "dashboards/people_members_tab" %>
        <% end %>
      </div>
    </section>
  </div>
</main>
